import { Card, CardContent, Typography } from "@mui/material";
import { Box } from "@mui/system";
import React from "react";

const orderTimeline = () => {
  return (
    <Box>
      <Card sx={{ height: "400px" }}>
        <CardContent>
          <Typography>orderTimeline</Typography>
        </CardContent>
      </Card>
    </Box>
  );
};

export default orderTimeline;
