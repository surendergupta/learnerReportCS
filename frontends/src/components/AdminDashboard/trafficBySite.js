import { Card, CardContent, Typography } from "@mui/material";
import { Box } from "@mui/system";
import React from "react";

const trafficBySite = () => {
  return (
    <Box>
      <Card sx={{ height: "400px" }}>
        <CardContent>
          <Typography>trafficBySite</Typography>
        </CardContent>
      </Card>
    </Box>
  );
};

export default trafficBySite;
