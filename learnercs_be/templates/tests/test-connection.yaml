apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "learnercs_be.fullname" . }}-test-connection"
  labels:
    {{- include "learnercs_be.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "learnercs_be.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
