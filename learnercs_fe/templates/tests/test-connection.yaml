apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "learnercs_fe.fullname" . }}-test-connection"
  labels:
    {{- include "learnercs_fe.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "learnercs_fe.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
